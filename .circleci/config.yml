version: 2

jobs:
    build:
        machine: true
        environment:
            - container: moses_service
        working_directory: ~/mozi_snet_service
        steps:
            - checkout
            - run:
                  name: Start the containers
                  command: docker-compose up -d
            - run:
                  name: Run tests with coverage
                  command: docker exec -it $container sh -c "coverage run --rcfile=./.coveragerc -m unittest && coverage report && coveralls"
workflows:
    version: 2
    build_and_test:
        jobs:
            - build
